// Default base URLs for chromeless embedded web-app tabs. Both are
// overridable via the Browser settings pane (persisted ui state
// `codeServerUrl` / `devinCloudUrl`).
export const DEFAULT_CODE_SERVER_URL = 'http://127.0.0.1:13337'
export const DEFAULT_DEVIN_CLOUD_URL = 'https://app.devin.ai'
